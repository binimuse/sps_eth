// ignore_for_file: constant_identifier_names

enum NetworkStatus { IDLE, LOADING, ERROR, SUCCESS }

enum ButtonSizeTypeLogin { LARGE, MEDIUM, SMALL }

enum ButtonSizeTypeSignup { LARGE, MEDIUM, SMALL }

enum ButtonSizeType { LARGE, MEDIUM, SMALL }

enum GraphQLCommonStatus { LOADING, ERROR }

enum ConfigurationRole { USER }

enum CheckBoxSize { SMALL, MEDIUM, LARGE }

enum VideoTypenew { video, image, audio, unknown }

enum DeviceType { phone, tablet }
